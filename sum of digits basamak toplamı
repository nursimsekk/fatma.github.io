# sum_of_digits.py
# This script calculates the sum of digits of a given number using a while loop.

n = 1346
sum = 0

while n > 0:
    r = n % 10         # Extract the last digit
    sum = sum + r      # Cumulative sum update
    n = n // 10        # Remove the last digit

print('Sum of Digits:', sum)
